---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"div">;

const props = Astro.props;
---

<div
  class:list={["mx-auto max-w-7xl px-4 sm:px-6 lg:px-8", props.class]}
  {...props}
>
  <slot />
</div>
